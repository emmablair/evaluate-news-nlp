import { handleSubmit } from '../js/formHandler';

describe('Handle submission for Sentiment API call', () => {
    test('grab all Sentiment APi info & handle submission functions', () => {
        expect(handleSubmit).toBeDefined();
    });
});