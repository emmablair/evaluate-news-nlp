import { updateUI } from '../js/updateUI';
import { updateSummary } from '../js/updateUI';

describe('Updates UI form on each submission', () => {
    test('Should update Sentiment form info', () => {
        expect(updateUI).toBeDefined();
    });
});

describe('Updates UI form on each submission', () => {
    test('Should update Summary form info', () => {
        expect(updateSummary).toBeDefined();
    });
});